apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: med-assert

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  # Uncomment after editing host/TLS:
  # - ingress.yaml

# Example: pin image without editing deployment.yaml
# images:
#   - name: med-assert
#     newName: ghcr.io/your-org/med-assert
#     newTag: "0.1.0"
